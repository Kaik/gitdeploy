#!/bin/bash

# load config

# check git

# check composer

# remove .git
rm -rf .git

# create bare repo for incoming
git init --bare deploy.git

# create post-received handler
mv post-receive/handler deploy.git/hooks/post-receive
chmod +x deploy.git/hooks/post-receive
# create working tree
mkdir workingtree

